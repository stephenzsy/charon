'use strict';
import {NetworkConfig} from '../lib/config/networks';

const NetworksConfig: NetworkConfig[] = require('../config/networks-config.json');

NetworksConfig.forEach((config: NetworkConfig) => { });
